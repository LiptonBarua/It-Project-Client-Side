import React from 'react';

const AboutImage = () => {
    return (
        <div className='h-[440px]' style={{backgroundImage: `url(https://www.itsolutions.ae/images/about-quote.png)`, backgroundRepeat: `no-repeat`, backgroundSize: `cover`}}>
            
        </div>
    );
};

export default AboutImage;